#include "main.h"
#include <string.h>

/**
 * _strcmp - compare
 * @s1: string 1
 * @s2: string 2
 * Return: val
 */
int _strcmp(char *s1, char *s2)
{
	int tmp;

	tmp = strcmp(s1, s2);
	return (tmp);
}
