#include "main.h"

/**
 * print_line - straight line using _.
 *  @n: _ characters to be printed.
 */
void print_line(int n)
{
	int len;

	if (n > 0)
	{
		for (len = 0; len < n;
			_putchar('_');
	}

	_putchar('\n');
}
